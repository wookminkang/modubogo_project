# 병원 공휴일 진료 일정 관리 & 알림톡 전송 기능

## 개요
매월 말, 다음달 공휴일을 공공데이터 API로 자동 조회하여
관리자에게 알림톡으로 안내하고, 알림톡 내 버튼을 통해
병원별 진료 여부를 설정할 수 있는 입력 폼 제공

---

## 전체 플로우

```
[자동화 / 어드민 수동 트리거]
  1. 다음달 공휴일 목록 조회 (공공데이터 API)
     예) 6월: 6일 현충일, 8일 지방선거, 18일 석가탄신일

  2. 관리자에게 알림톡 전송
     내용: "다음달 공휴일이 ○건 있습니다. 진료 여부를 입력해주세요."
     버튼: [휴무날짜 체크하기] → /report/[company]/holiday 링크

[관리자]
  3. 알림톡 수신 → [휴무날짜 체크하기] 버튼 클릭
     → /report/[company]/holiday 페이지 이동

  4. 공휴일별 진료 여부 선택
     - 정상 진료
     - 휴무
     - 단축 진료 → 시작/종료 시간 입력 필드 노출

  5. 저장 → DB에 병원별 공휴일 일정 저장

  6. 환자 알림톡 전송
     → 설정된 수신자(recipient1~3)에게 알림톡 발송

[환자/수신자]
  → 알림톡 수신
     "[병원명] 6월 공휴일 진료 안내"
     내용: 날짜별 정상/휴무/단축 정보
```

---

## DB 설계

### 신규 테이블: `holiday_schedules`

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | int8 (PK) | |
| company | text | 병원명 (company_settings.company) |
| date | date | 공휴일 날짜 |
| holiday_name | text | 공휴일명 (예: 현충일) |
| status | text | open / closed / short |
| short_start | text | 단축 시작 시간 (예: 09:00), status=short일 때 |
| short_end | text | 단축 종료 시간 (예: 13:00), status=short일 때 |
| note | text | 추가 메모 (선택) |
| created_at | timestamptz | |

```sql
CREATE TABLE holiday_schedules (
  id bigint PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
  company text NOT NULL,
  date date NOT NULL,
  holiday_name text NOT NULL,
  status text NOT NULL DEFAULT 'open', -- open | closed | short
  short_start text,
  short_end text,
  note text,
  created_at timestamptz DEFAULT now(),
  UNIQUE (company, date)
);
```

---

## 공휴일 데이터 소스

### 공공데이터포털 API
- URL: `https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService/getRestDeInfo`
- 파라미터: `solYear`, `solMonth`, `ServiceKey`
- 무료, 월별 조회 가능, XML 응답

---

## 화면 구성

### 1. `/report/[company]` 병원 상세 페이지

기존 설정 버튼 열에 버튼 추가:

```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 알림톡 설정   │ │ 네이버 API   │ │ 데이블 설정  │ │공휴일 진료설정│
└──────────────┘ └──────────────┘ └──────────────┘ └──────────────┘
```

버튼 클릭 시 → `/report/[company]/holiday` 페이지로 이동  
(또는 어드민 알림톡 수동 발송 기능 포함)

---

### 2. 관리자 수신 알림톡 (관리자 대상)

```
┌──────────────────────────────────────┐
│  [모두보고] 공휴일 진료 설정 안내      │
│                                      │
│  [병원명] 원장님, 안녕하세요.          │
│  다음달 공휴일이 3건 있습니다.          │
│  진료 여부를 설정해주세요.             │
│                                      │
│  📅 6월 6일 (목) 현충일               │
│  📅 6월 8일 (토) 지방선거             │
│  📅 6월 18일 (목) 석가탄신일          │
│                                      │
│  ┌──────────────────────────────┐    │
│  │       휴무날짜 체크하기        │    │
│  └──────────────────────────────┘    │
└──────────────────────────────────────┘
```

버튼 URL: `https://modubogo.com/report/[company]/holiday`

---

### 3. 공휴일 설정 페이지 `/report/[company]/holiday`

모바일 기준 (max-w-[420px]):

```
┌─────────────────────────────────────┐
│  ← [병원명]                          │
│  6월 공휴일 진료 설정                  │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │ 📅 6월 6일 (목) 현충일         │  │
│  │                               │  │
│  │  ○ 정상 진료                  │  │
│  │  ● 휴무                       │  │
│  │  ○ 단축 진료                  │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ 📅 6월 8일 (토) 지방선거       │  │
│  │                               │  │
│  │  ● 정상 진료                  │  │
│  │  ○ 휴무                       │  │
│  │  ○ 단축 진료                  │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │ 📅 6월 18일 (목) 석가탄신일    │  │
│  │                               │  │
│  │  ○ 정상 진료                  │  │
│  │  ○ 휴무                       │  │
│  │  ● 단축 진료                  │  │
│  │                               │  │
│  │  시작   [09:00]  ~  [13:00]   │  │  ← 단축 선택 시만 노출
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────┐  ┌─────────────────┐ │
│  │   저장    │  │ 환자 알림톡 전송  │ │
│  └───────────┘  └─────────────────┘ │
└─────────────────────────────────────┘
```

동작:
- 단축 진료 라디오 선택 시 → 시작/종료 시간 입력 필드(HH:MM) 노출
- 다른 옵션 선택 시 → 필드 숨김, 시간값 초기화
- [저장] → DB upsert, 성공 토스트 표시
- [환자 알림톡 전송] → 저장 후 수신자에게 알림톡 발송, 로그 저장

---

### 4. 환자 알림톡 (환자 대상)

```
┌──────────────────────────────────────┐
│  [병원명] 6월 공휴일 진료 안내          │
│                                      │
│  안녕하세요, [병원명]입니다.            │
│  6월 공휴일 진료 일정을 안내드립니다.   │
│                                      │
│  📅 6월 6일 (목) 현충일               │
│     → 휴무                           │
│                                      │
│  📅 6월 8일 (토) 지방선거             │
│     → 정상 진료                      │
│                                      │
│  📅 6월 18일 (목) 석가탄신일          │
│     → 단축 진료 (09:00 ~ 13:00)      │
│                                      │
│  문의: 02-XXXX-XXXX                  │
└──────────────────────────────────────┘
```

---

## 구현 순서

### Phase 1 - 데이터 기반
- [ ] Supabase `holiday_schedules` 테이블 생성 (SQL 실행)
- [ ] 공공데이터 API 키 발급 및 `getPublicHolidays(year, month)` 함수 구현
- [ ] DB 저장/조회 함수 (`db.ts`에 추가)

### Phase 2 - 관리자용 알림톡
- [ ] 관리자 알림톡 템플릿 bizgo 등록 (공휴일 목록 + 체크하기 버튼 포함)
- [ ] 알림톡 전송 함수 구현 (`sendHolidayCheckAlimtalk`)
- [ ] 트리거: 매월 25일 자동 발송 또는 어드민 수동 발송 버튼

### Phase 3 - 공휴일 설정 폼
- [ ] `/report/[company]/holiday/page.tsx` 서버 페이지 (공휴일 목록 + 기존 설정값 조회)
- [ ] `HolidayForm.tsx` 클라이언트 컴포넌트
  - 각 공휴일마다 정상/휴무/단축 라디오 버튼
  - 단축 선택 시 시작/종료 시간 입력 필드 노출
- [ ] 저장 server action (`upsertHolidaySchedules`)

### Phase 4 - 환자 알림톡 전송
- [ ] 환자 알림톡 템플릿 bizgo 등록
- [ ] 전송 로직 구현 (`sendHolidayPatientAlimtalk`)
- [ ] 전송 버튼 및 로그 저장

---

## 파일 구조 (예상)

```
src/
  app/
    report/[company]/
      holiday/
        page.tsx          # 공휴일 설정 페이지 (서버)
        HolidayForm.tsx   # 클라이언트 폼 (라디오 + 시간 입력)
        actions.ts        # 저장 / 알림톡 전송 server action
  lib/
    publicHoliday.ts      # 공공데이터 API 호출
    db.ts                 # getHolidaySchedules, upsertHolidaySchedule 추가
```
